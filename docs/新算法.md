1、当前顶部边缘的两个3D点，可以确定一个向上的方向。两个点之差可以确定一个线段的长度 A。    
2、用第一步骤确定的方向和长度A，生成两个点出来。 两个点的方向都不变，第一个点的长度为0.8A ，第二个点的长度为1.6A。这两个长度需要在web界面里面课时实时调整看效果。
3、[68, 54]，[104, 103]，[69, 67]，[108, 109]，[151, 10]，[337, 338]，[299, 297]，[333, 332]，[298, 284] 从这9组点往外延申出去，总共18个点。
4、在原有face.obj 3D模型的基础加上这18个点，并且分配好三角面，拓扑好UV, 输出一个新的obj模型。
5、上传人脸照片，先用Mediapipe 检测出初步的468个点，然后安装刚才的算法扩展到486个点。调整新的obj模型点的位置。
6、输出照片的基础上，再用刚才调整过点的新模型 用texture0.png 这个作为贴图 渲染叠加上去。