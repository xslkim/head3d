# Core (build_extended_obj.py only needs numpy)
numpy>=1.24

# For the full extract_hairline.py pipeline:
opencv-python>=4.8
mediapipe>=0.10
Pillow>=10.0
Flask>=3.0

# Face parsing (HuggingFace SegFormer). Will auto-download ~150MB weights on first run.
torch>=2.0
torchvision>=0.15  # transformers' SegformerImageProcessor needs this
transformers>=4.40
